"use client";
import {FormEvent,useState} from "react";
export function ContactForm(){const[state,setState]=useState("idle");async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setState("sending");const form=e.currentTarget;const r=await fetch("/api/enquiries",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(Object.fromEntries(new FormData(form).entries()))});if(r.ok){form.reset();setState("sent")}else setState("error")}return <form className="form-card" onSubmit={submit}><div className="grid-2"><label>Name<input name="name" required placeholder="Your name"/></label><label>Email<input name="email" type="email" required placeholder="you@example.com"/></label></div><div className="grid-2"><label>Phone<input name="phone" placeholder="050..."/></label><label>Company<input name="company" placeholder="Business name"/></label></div><div className="grid-2"><label>Service<select name="service" defaultValue=""><option value="" disabled>Select a service</option><option>Website Development</option><option>Web App / SaaS</option><option>Business Automation</option><option>E-commerce</option><option>Maintenance</option></select></label><label>Budget<select name="budget" defaultValue=""><option value="" disabled>Choose a range</option><option>Under GHS 2,000</option><option>GHS 2,000 – 5,000</option><option>GHS 5,000 – 10,000</option><option>GHS 10,000+</option></select></label></div><label>Project details<textarea name="message" required rows={6} placeholder="Tell us what you want to build..."/></label><button className="button" disabled={state==="sending"}>{state==="sending"?"Sending...":"Send project enquiry"}</button>{state==="sent"&&<p className="success">Enquiry received. We’ll get back to you shortly.</p>}{state==="error"&&<p className="error">Could not save the enquiry. Check the database setup.</p>}</form>}