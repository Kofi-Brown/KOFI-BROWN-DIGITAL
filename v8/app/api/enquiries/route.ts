import {NextResponse} from "next/server"; import {prisma} from "@/lib/prisma";
export async function POST(request:Request){try{const b=await request.json();if(!b?.name||!b?.email||!b?.message)return NextResponse.json({error:"Name, email and message are required."},{status:400});const e=await prisma.enquiry.create({data:{name:String(b.name).trim(),email:String(b.email).trim(),phone:b.phone?String(b.phone).trim():null,company:b.company?String(b.company).trim():null,service:b.service?String(b.service).trim():null,budget:b.budget?String(b.budget).trim():null,message:String(b.message).trim()}});return NextResponse.json({ok:true,id:e.id},{status:201})}catch{return NextResponse.json({error:"Database unavailable or not configured."},{status:500})}}