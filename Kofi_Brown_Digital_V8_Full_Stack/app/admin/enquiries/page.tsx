import {redirect} from "next/navigation"; import {prisma} from "@/lib/prisma"; import {isAdmin} from "@/lib/session";
export default async function Enquiries(){if(!(await isAdmin()))redirect("/admin/login");const rows=await prisma.enquiry.findMany({orderBy:{createdAt:"desc"}});return <main className="admin-shell"><div className="dashboard-top"><div><span className="eyebrow">CRM</span><h1>Enquiries</h1></div><a className="button secondary" href="/admin">Dashboard</a></div><div className="dashboard-card" style={{overflowX:"auto"}}><table className="table"><thead><tr><th>Name</th><th>Contact</th><th>Service</th><th>Budget</th><th>Status</th><th>Created</th></tr></thead><tbody>{rows.map(x=><tr key={x.id}><td>{x.name}<br/><span className="small">{x.company||""}</span></td><td>{x.email}<br/>{x.phone||""}</td><td>{x.service||"—"}</td><td>{x.budget||"—"}</td><td><span className="pill">{x.status}</span></td><td>{x.createdAt.toLocaleString()}</td></tr>)}</tbody></table></div></main>}