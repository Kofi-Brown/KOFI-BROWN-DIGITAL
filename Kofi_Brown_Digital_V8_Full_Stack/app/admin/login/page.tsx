"use client"; import {FormEvent,useState} from "react"; import {useRouter} from "next/navigation";
export default function Login(){const router=useRouter();const[error,setError]=useState("");async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setError("");const r=await fetch("/api/admin/login",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(Object.fromEntries(new FormData(e.currentTarget).entries()))});if(r.ok)router.push("/admin");else setError("Invalid admin credentials.");}return <main className="login"><div className="card"><span className="tag">PRIVATE AREA</span><h1>Admin login</h1><p className="small">Credentials come from your environment variables.</p><form className="form-card" onSubmit={submit}><label>Email<input name="email" type="email" required/></label><label>Password<input name="password" type="password" required/></label><button className="button">Sign in</button>{error&&<p className="error">{error}</p>}</form></div></main>}