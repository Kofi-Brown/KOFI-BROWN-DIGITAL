import Link from "next/link"; import {redirect} from "next/navigation"; import {prisma} from "@/lib/prisma"; import {isAdmin} from "@/lib/session";
export default async function Admin(){if(!(await isAdmin()))redirect("/admin/login");const[e,c,p,pr]=await Promise.all([prisma.enquiry.count(),prisma.client.count(),prisma.project.count(),prisma.proposal.count()]);const recent=await prisma.enquiry.findMany({orderBy:{createdAt:"desc"},take:8});return <main className="admin-shell"><div className="dashboard-top"><div><span className="eyebrow">Kofi Brown Digital OS</span><h1>Command center</h1></div><form action="/api/admin/logout" method="post"><button className="button secondary">Sign out</button></form></div><div className="metrics"><div className="dashboard-card"><span className="small">Enquiries</span><strong>{e}</strong></div><div className="dashboard-card"><span className="small">Clients</span><strong>{c}</strong></div><div className="dashboard-card"><span className="small">Projects</span><strong>{p}</strong></div><div className="dashboard-card"><span className="small">Proposals</span><strong>{pr}</strong></div></div><section style={{padding:"40px 0"}}><div className="section-head"><h2>Pipeline</h2><p><Link href="/admin/enquiries">Open all enquiries →</Link></p></div><div className="dashboard-card" style={{overflowX:"auto"}}><table className="table"><thead><tr><th>Client</th><th>Service</th><th>Status</th><th>Date</th></tr></thead><tbody>{recent.map(x=><tr key={x.id}><td>{x.name}<br/><span className="small">{x.email}</span></td><td>{x.service||"—"}</td><td><span className="pill">{x.status}</span></td><td>{x.createdAt.toLocaleDateString()}</td></tr>)}</tbody></table></div></section></main>}